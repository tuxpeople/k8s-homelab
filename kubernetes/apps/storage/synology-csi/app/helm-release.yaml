---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app synology-csi
spec:
  interval: 30m
  chart:
    spec:
      chart: synology-csi
      version: 0.10.1
      sourceRef:
        kind: HelmRepository
        name: synology-csi
        namespace: flux-system
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    remediation:
      retries: 3
  values:
    deployNodeAgent: true
    nodeAgent:
      containerSecurityContext:
        privileged: true
    configuration:
      features: EnableCSI
      backupStorageLocation:
        - bucket: *app
          name: default
          provider: aws
          prefix: csi
          default: true
          config:
            region: minio
            s3ForcePathStyle: true
            s3Url: http://minio.lab.tdeutsch.ch:9091
      volumeSnapshotLocation:
        - name: default
          provider: csi
    initContainers:
      - name: synology-csi-plugin-for-aws
        image: synology-csi/synology-csi-plugin-for-aws:v1.12.1
        volumeMounts:
          - mountPath: /target
            name: plugins
    credentials:
      useSecret: true
      existingSecret: minio-secrets
