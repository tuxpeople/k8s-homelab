---
# yaml-language-server: $schema=https://kubernetes-schemas.pages.dev/helm.toolkit.fluxcd.io/helmrelease_v2.json
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: &app postgres-operator
spec:
  chart:
    spec:
      chart: postgres-operator
      version: 1.15.1
      sourceRef:
        kind: HelmRepository
        name: postgres-operator
        namespace: flux-system
  interval: 10m
  # timeout: 30m
  test:
    enable: false
  install:
    remediation:
      retries: -1
  upgrade:
    remediation:
      retries: 5
      remediateLastFailure: true
    cleanupOnFail: true
  rollback:
    timeout: 10m
    cleanupOnFail: false
  values:
    configGeneral:
      repair_period: 1m
      resync_period: 5m
      workers: 32
    configMajorVersionUpgrade:
      major_version_upgrade_mode: manual
      # configKubernetes:
      #   pod_environment_secret: pod-config
# Ergänzen siehe https://github.com/geraldwuhoo/homelab-iac/blob/cd2ba8cd221ebcb96f3c78f8ddcee16c9bf411d1/ # yamllint disable-line rule:line-length
# fluxcd/core/production/postgres-operator/secret.yaml#L4
