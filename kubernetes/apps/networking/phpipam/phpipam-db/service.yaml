apiVersion: v1
kind: Service
metadata:
  name: phpipam-mysql-service
spec:
  selector:
    app: phpipam
    component: mysql
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 3306
      targetPort: 3306
