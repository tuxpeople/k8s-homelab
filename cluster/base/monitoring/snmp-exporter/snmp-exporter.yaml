---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/instance: snmp-exporter
    app.kubernetes.io/name: snmp-exporter
  name: snmp-exporter
  namespace: monitoring
spec:
  clusterIP: None
  ports:
  - name: monitoring
    port: 9116
  selector:
    app.kubernetes.io/instance: snmp-exporter
    app.kubernetes.io/name: snmp-exporter
