---
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: fluxcdbot
  namespace: flux-system
  labels:
    app.kubernetes.io/name: fluxcdbot
spec:
  template:
    spec:
      containers:
      - name: fluxcdbot
        env:
        - name: TOKEN
          valueFrom:
            secretKeyRef:
              name: telegram-token
              key: token
