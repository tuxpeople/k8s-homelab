---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: gollum
  name: gollum
  namespace: gollum
spec:
  ports:
  - name: http
    port: 8080
    protocol: TCP
    targetPort: 8080
  selector:
    app: gollum
  type: ClusterIP
