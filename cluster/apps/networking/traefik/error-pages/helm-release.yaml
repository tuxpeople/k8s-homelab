---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: error-pages
  namespace: networking
spec:
  interval: 5m
  chart:
    spec:
      # renovate: registryUrl=https://helm.error-pages.io/error-pages
      chart: error-pages
      version: 0.1.2
      sourceRef:
        kind: HelmRepository
        name: kiwigrid
        namespace: flux-system
      interval: 5m
  dependsOn:
    - name: traefik
      namespace: networking
