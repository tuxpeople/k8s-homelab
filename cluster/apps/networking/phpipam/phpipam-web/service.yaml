apiVersion: v1
kind: Service
metadata:
  name: phpipam-service
spec:
  selector:
    app: phpipam
    component: www
  type: LoadBalancer
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
