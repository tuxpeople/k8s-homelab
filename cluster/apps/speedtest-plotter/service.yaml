---
apiVersion: v1
kind: Service
metadata:
  labels:
    app: speedtest-plotter
  name: speedtest-plotter
  namespace: speedtest-plotter
spec:
  ports:
  - name: http
    port: 8000
    protocol: TCP
    targetPort: 8000
  selector:
    app: speedtest-plotter
  type: ClusterIP
