apiVersion: v1
kind: ConfigMap
metadata:
  name: docs-watch-script
data:
  watch.sh: |-
    #!/bin/sh
    while [ ! -f /git/current/mkdocs.yml ]
    do
      sleep 2
    done
    mkdocs serve --config-file /git/current/mkdocs.yml -a 0.0.0.0:8000
