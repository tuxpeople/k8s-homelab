---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: development
  labels:
    app: gollum
  name: gollum-data
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
  storageClassName: "portworx-csi-sc-replicated"
