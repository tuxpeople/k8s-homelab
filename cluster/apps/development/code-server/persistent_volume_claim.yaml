---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: development
  labels:
    app: code-server
  annotations:
    k8up.io/backup: "true"
  name: code-server-data-pvc
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 5Gi
