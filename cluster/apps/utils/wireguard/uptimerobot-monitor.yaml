---
apiVersion: uroperator.brennerm.github.io/v1beta1
kind: UptimeRobotMonitor
metadata:
  name: wireguard
  namespace: utils
spec:
  friendlyName: "K8S wireguard"
  url: "vpn.${SECRET_DOMAIN_ME}"
  type: PORT
  subType: CUSTOM
  port: 51820
  interval: 300
  alertContacts: "${SECRET_UPTIMEROBOTCONTACTS}"
