---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: semaphore
  name: semaphore-data
  namespace: ansible-semaphore
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
