---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    external-dns.alpha.kubernetes.io/target: ${SECRET_DOMAIN_TARGET_SKY}
    external-dns/is-public: true
    kubernetes.io/ingress.class: traefik
    traefik.ingress.kubernetes.io/redirect-entry-point: https
  labels:
    app: podsync
  name: podsync-ingress
  namespace: media
spec:
  rules:
  - host: podsync.${SECRET_DOMAIN_ME}
    http:
      paths:
      - backend:
          service:
            name: podsync-svc
            port:
              number: 8080
        path: /
        pathType: Prefix
  tls:
  - hosts:
    - podsync.${SECRET_DOMAIN_ME}
    secretName: podsync-eighty-three-me-tls
