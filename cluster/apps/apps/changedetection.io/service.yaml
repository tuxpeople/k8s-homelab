---
apiVersion: v1
kind: Service
metadata:
  namespace: apps
  name: changedetection
  labels:
    app: changedetection
spec:
  type: ClusterIP
  ports:
    - port: 5000
      targetPort: 5000
      protocol: TCP
      name: http
  selector:
    app: changedetection
