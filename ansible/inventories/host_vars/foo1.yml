keepalived_vrrp_instances:
  - name: VI_1
    state: MASTER
    interface: eth0
    virtual_router_id: 51
    priority: 255
    authentication:
      auth_type: PASS
      auth_pass: "{{ vrrp_password }}"
    virtual_ipaddresses:
      - name: "{{ vrrp_vip }}"
        cidr: 24