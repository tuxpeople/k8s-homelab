---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: prometheus-uptimerobot-exporter
  namespace: monitoring
spec:
  releaseName: prometheus-uptimerobot-exporter
  chart:
    spec:
      # renovate: registryUrl=https://k8s-at-home.com/charts
      chart: prometheus-uptimerobot-exporter
      version: 3.0.0
      sourceRef:
        kind: HelmRepository
        name: k8s-at-home
        namespace: flux-system
  interval: 5m
  install:
    remediation:
      retries: 3
  values:
    prometheus:
      serviceMonitor:
        enabled: true
  valuesFrom:
  - secretKeyRef:
    kind: Secret
    name: prometheus-uptimerobot-exporter-values