apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: k8up
  namespace: k8up-operator
spec:
  chart:
    spec:
      # renovate: registryUrl=https://bitnami-labs.github.io/k8up
      chart: k8up
      version: 1.0.0
      sourceRef:
        kind: HelmRepository
        name: appuio
        namespace: flux-system
  interval: 1h0m0s
  releaseName: k8up-operator
  targetNamespace: k8up-operator
  install:
    crds: Create
  upgrade:
    crds: CreateReplace
