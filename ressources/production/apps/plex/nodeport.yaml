apiVersion: v1
kind: Service
metadata:
  namespace: plex
  name: plex-nodeport
  labels:
    app.kubernetes.io/instance: plex
    app.kubernetes.io/name: plex
spec:
  type: NodePort
  ports:
    - name: web
      nodePort: 32400
      port: 32400
      protocol: TCP
      targetPort: 32400
  selector:
    app.kubernetes.io/instance: plex
    app.kubernetes.io/name: plex