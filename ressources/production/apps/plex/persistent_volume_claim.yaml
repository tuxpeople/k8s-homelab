apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  namespace: plex
  labels:
    app: plex
  name: config
spec:
  accessModes:
  - ReadWriteOnce
  #storageClassName: nfs-client
  resources:
    requests:
      storage: 5Gi
