apiVersion: v1
kind: Service
metadata:
  namespace: gollum
  name: gollum
  labels:
    app: gollum
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
  selector:
    app: gollum

