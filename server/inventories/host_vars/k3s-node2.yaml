---
keepalived_vrrp_instances:
  - name: VI_1
    state: BACKUP
    interface: eth0
    virtual_router_id: 51
    priority: 254
    authentication:
      auth_type: PASS
      auth_pass: "{{ vrrp_password }}"
    virtual_ipaddresses:
      - name: "{{ vrrp_vip }}"
        cidr: 24
