apiVersion: helm.cattle.io/v1
kind: HelmChartConfig
metadata:
  name: traefik
  namespace: kube-system
spec:
  valuesContent: |-
    additionalArguments:
      - --serversTransport.insecureSkipVerify=true
    dashboard:
      enabled: true
      domain: "traefik.192.168.8.222.nip.io"
